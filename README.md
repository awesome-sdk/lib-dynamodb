# lib-dynamodb
AI-generated: A drop-in replacement for @aws-sdk/lib-dynamodb with optional, incremental adoption of schema-first entities (Zod) for type-safe key generation and domain mapping—while keeping the familiar DynamoDBDocumentClient.send(new Command()) API.
